// Sólo se usa en web (TripDetails, paso Fecha y hora). En nativo el picker es
// @react-native-community/datetimepicker; este archivo existe para que el import
// resuelva y nunca se renderiza (ver DateTimeRow.web.js).
export default function DateTimeRow() {
  return null;
}
