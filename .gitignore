node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env

# capturas para debug, no van al bundle
assets/Nueva carpeta/

# Carpetas nativas: las regenera `expo prebuild` en cada build a partir de app.json.
# Tenerlas commiteadas hacia que EAS salteara prebuild y que TODA la config de
# android en app.json se ignorara (permisos, plugins, splash, google-services).
/android/
/ios/
